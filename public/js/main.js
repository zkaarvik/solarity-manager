//Declare the module
var solarityManager = angular.module('solarityManager', ['ui.bootstrap']);

//Configure module

solarityManager.controller('MainController', function($scope) {
	$scope.buttonModel = true;
});